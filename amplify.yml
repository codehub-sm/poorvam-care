version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - echo "NODE_ENV=production"
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
    discard-paths: no 